---
layout: docs
title: "Python & Analysis test"
---

# Питон 4. Анализ и визуализация данных с помощью numpy, pandas, matplotlib

### Вопрос 1. 

Что будет при запуске приведенного ниже кода?

```python
a = np.array([2,12,85,0,6])
a + 5
```

1. ```[2, 12, 85, 0, 6, 5]```
2. ```array([ 7, 17, 90,  5, 11])```
3. Выдает ошибку ```TypeError: can only concatenate list (not "int") to list```
4. ```110```


### Вопрос 2. 

Выберите правильный вариант кода для загрузки файла ```file.csv``` со следующим содержимым:

```
приборы|8
тангаж|11.5
развитие|0.05
температура|11.1
макроэкономика|9
инфлюэнца|100
декупаж|9.99
```

1. ```df = pd.read_csv('file.csv', header=None)```

2. ```df = pd.read_csv('file.csv', sep='|')```

3. Правильный вариант отсутствует

4. ```df = pd.read_csv('file.csv', header=None, sep='|')```


### Вопрос 3.

Задан следующий массив numpy ```A```:
```
[[10 20 30]
 [40 50 60]
 [70 80 90]]
```

Что будет вывдено на экран при запуске следующего фрагмента кода:
```python
print(A > 50)
```

1. ```
[[nan nan nan]
 [nan 50. 60.]
 [70. 80. 90.]]
 ```

2. ```
[[False False False]
 [False False  True]
 [ True  True  True]]
 ```

3. ```
[60 70 80 90]
```

4. Ничего из приведенного выше.

### Вопрос 4.

Какая функция pandas используется для создания сводных таблиц?

1. ```python
df.pivot_table()
```

2.  ```python
pd.pivot()
```

3. ```python
pd.unstack()
```

4. ```python
pd.read_excel()
```


### Вопрос 5.

Как правильно вызвать функцию перемножения матриц в numpy?

1. ```python
np.dot(A, B)
```

2. ```python
A.dot(B)
```

3. ```python
A @ B
```

4. Все варианты правильны


### Вопрос 6.


Какой вариант кода из приведенных ниже работать быстрее всего? Данный код вычисляет сумму квадратов последовательности чисел от 0 до 1000.

```python
x = sum(x*x for x in range(1000)) # 1
```

```python
na = np.arange(1000)
x = sum(na*na) # 2
```

```python
na = np.arange(1000)
x = np.dot(na, na) # 3
```

1. Первый

2. Второй

3. Третий

4. Производительность кода зависит от входных данных, здесь каждый вариант будет иметь свои преимущества в зависимости от того, что подано на вход.



### Вопрос 7.


Требуется построить график функции ```y=f(x)```. Как правильно задать множество значений по оси ```x``` от -3 до 3 включительно?

1. ```python
x = np.arange(-3, 3, 0.1)
```

2. ```python
x = np.linspace(-3, 3, 60)
```

3. Ни один из представленных выше

4. Оба варианта правильные


### Вопрос 8.

У вас есть следующий DataFrame ```df```:
```
      values
Joe        5
Jim       10
Leah      15
```

Какой вариант кода построит столбчатую диаграмму для этих данных?

1. ```
plt.bar(df.index, df['values'])
```

2. ```
df['values'].bar()
```

3. ```
plt.plot(df['values'])
```


### Вопрос 9.

У вас есть следующий DataFrame ```purchases```:
```
         apples  oranges
Carl        3        0
Julia       2        3
Alex        0        7
Leela       1        2
```

Какой вариант кода отобразит тех, кто не любит яблоки или апельсины?

1. ```
purchases.loc[ (purchases['apples'] == 0) | (purchases['oranges'] == 0) ]
```

2. ```
purchases.loc[ purchases['apples' or 'oranges'] == 0 ]
```

3. ```
purchases[ purchases['apples'] == 0 ] + purchases[ purchases['oranges'] == 0 ]
```

### Вопрос 10.

У вас есть вектор ```a```: ```[2, 12, 85, 0, 6]```.

Выберите правильный вариант кода, который сделает из него следующий массив:
```
[[ 2]
 [12]
 [85]
 [ 0]
 [ 6]]
```


1. ```
a.T
```

2. ```
a.reshape(-1, 1)
```

3. ```
np.expand_dims(a, axis=0)
```

4. Второй или третий варианты

5. Все варианты правильные
